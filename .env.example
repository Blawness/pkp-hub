# Database (Drizzle → local Postgres in dev, Neon in prod)
DATABASE_URL="postgresql://user:password@localhost:5432/pkp_hub"

# Auth (Better Auth) — generate a secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# File storage (Cloudflare R2, S3-compatible) — required from Phase 4
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET=""
R2_PUBLIC_URL=""

# Email for client portal invites (Resend) — required from Phase 2
RESEND_API_KEY=""
